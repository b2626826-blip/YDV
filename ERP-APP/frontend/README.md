# YDV ERP Frontend

React 19 + TypeScript + Vite + Tailwind CSS 的 PWA 前端。

```bash
npm install
npm run dev
```

目前正式入口在 `src/app/`，第一個業務切片在 `src/features/production/`。API base URL 可用 `VITE_API_BASE_URL` 設定，預設為 `http://localhost:8080`。

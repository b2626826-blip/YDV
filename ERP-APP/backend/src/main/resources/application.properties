spring.application.name=ydv-erp-api
server.port=${SERVER_PORT:8080}

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:55432/ydv_erp}
spring.datasource.username=${DB_USERNAME:ydv}
spring.datasource.password=${DB_PASSWORD:ydv_dev_only}

spring.flyway.enabled=${FLYWAY_ENABLED:true}
app.frontend-origin=${FRONTEND_ORIGIN:http://localhost:5173}

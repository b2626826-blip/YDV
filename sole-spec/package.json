{
  "name": "sole-spec",
  "version": "1.0.0",
  "private": true,
  "description": "鞋底材料規格對照表 — 資料驅動的單頁速查表",
  "type": "module",
  "scripts": {
    "build": "tsx build.ts",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test src/**/*.test.ts",
    "check": "npm run typecheck && npm test && npm run build"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
